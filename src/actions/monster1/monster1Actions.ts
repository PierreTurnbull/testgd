import { RollingAction } from "./rolling/rolling.action"
import { StandingAction } from "./standing/standing.action"

export const monster1Actions = {
	Standing: StandingAction,
	Rolling: RollingAction,
}
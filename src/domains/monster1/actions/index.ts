import { RollingAction } from "./rolling/rolling.actions";
import { StandingAction } from "./standing/standing.actions";

export const monster1Actions = {
	Standing: StandingAction,
	Rolling: RollingAction,
};
import { attackingTextures } from "./attacking"
import { runningTextures } from "./running"
import { standingTextures } from "./standing"

export const playerTextures = {
	running: runningTextures,
	standing: standingTextures,
	attacking: attackingTextures,
}
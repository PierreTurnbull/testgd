export const DIAGONAL_DISTANCE_FACTOR = Math.sqrt(1 / 2);
/**
 * The scale factor used to scale the sprites, making small pixels larger.
 * This is used to load smaller images, thus improving performances.
 */
export const SCALE_FACTOR = 3;